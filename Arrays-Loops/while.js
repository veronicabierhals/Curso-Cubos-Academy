const nomes = ['Jose', 'João' , 'Maria', 'Joana', 'André'];

let indice = 0;

while (indice < nomes.length) { //enquanto indice for menor que tamanho da lista
    console.log(nomes[indice]);
    indice++; //acrescenta 1 a indice
} 

//utilizado para verificar se uma variável que guarda uma string está vazia
const nome = ''; //string vazio

if (!nome) { // == a -> if (nome === '') -> se nome diferente de string vazio
    console.log('Por favor, preencha seu nome.');
}
//programa que imprime todos os elementos de um array que contém números quaisquer.

//for
const numeros = [27, 10, 33, 32, 1];

for (let i = 0; i < numeros.length; i++) {
    console.log(numeros[i]);
}

//while
let i = 0;
while (i < numeros.length) {
    console.log(numeros[i]);
    i++;
}